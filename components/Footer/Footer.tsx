import footer from './FooterStyles';
import { View, Text } from 'react-native';

const Footer = () => {
  return (
    <View style={footer}>
      <Text>Footer</Text>
    </View>
  );
};

export default Footer;
